global:
  domain: argocd.localhost

configs:
  params:
    server.insecure: true

server:
  service:
    type: ClusterIP
  extraArgs:
    - --insecure

controller:
  replicas: 1

repoServer:
  replicas: 1

applicationSet:
  enabled: true

notifications:
  enabled: false

